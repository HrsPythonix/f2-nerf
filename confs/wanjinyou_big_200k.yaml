defaults:
  - train: 200k
  - dataset: default
  - renderer: default
  - pts_sampler: perspective
  - field: hash3d_anchored
  - shader: sh_shader
  - _self_

dataset_name: example
case_name: tmp
exp_name: test

is_continue: false

mode: train

field:
  log2_table_size: 21

renderer:
  # Appearance embedding to handling unstable lighting conditions
  use_app_emb: false

pts_sampler:
  split_dist_thres: 1.5
  near: 0.01
  scale_by_dis: true
  sub_div_milestones: [ 20000, 40000, 60000, 80000, 100000 ]

dataset:
  factor: 2
  bounds_factor: [ 0.5, 4.0 ]

train:
  ray_march_init_fineness: 16

