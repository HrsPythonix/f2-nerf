defaults:
  - train: 100k
  - dataset: default
  - renderer: default
  - pts_sampler: perspective
  - field: hash3d_anchored
  - shader: sh_shader
  - _self_

dataset_name: postbank
case_name: postbank_counter_clip2_a7s3_4k_800
exp_name: 1080p_100k_20ts_l1

is_continue: false

mode: train

field:
  log2_table_size: 20

renderer:
  # Appearance embedding to handling unstable lighting conditions
  use_app_emb: true

pts_sampler:
  split_dist_thres: 1.5
  near: 0.01
  scale_by_dis: true

dataset:
  factor: 2
  bounds_factor: [ 0.5, 4.0 ]

train:
  ray_march_init_fineness: 16

